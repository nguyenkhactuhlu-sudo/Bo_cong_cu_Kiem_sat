# Build artifacts
build/
dist/
__pycache__/

# Spec files in build/dist (build artifacts)
build/*.spec
dist/*.spec

# Executable files (too large for git)
*.exe

# Zip archives in build/dist (build artifacts, too large for git)
build/*.zip
dist/*.zip

# Python cache
*.pyc
*.pyo

# API Keys (chứa thông tin nhạy cảm)
Tool/PdfToMd/API keys.txt
